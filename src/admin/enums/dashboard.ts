export enum AddressValidationMessages {
  InvalidAddToWhiteList = "Please supply a valid address to whitelist",
  InvalidRemoveFromWhiteList = "Please supply a valid address to remove from whitelist",
  InvalidRetrieveProjectToken = "Please supply a valid address to whitelist",
  InvalidAddress = "Invalid Address",
}

export enum DashboardActions {
  AddToWhiteList = "ADD_TO_WHITELIST",
  RemoveFromWhiteList = "REMOVE_FROM_WHITELIST",
  RetrieveProjectToken = "RETRIEVE_PROJECT_TOKEN",
}
